[project]
name = "five"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=1.21.0",
    "mcp>=1.10.1",
    "tomli-w>=1.2.0",
    "tqdm>=4.67.1",
]

[tool.isort]
profile = "black"

[tool.black]
line-length = 90

[dependency-groups]
dev = [
    "black>=25.1.0",
    "invoke>=2.2.0",
    "isort>=6.0.1",
]
