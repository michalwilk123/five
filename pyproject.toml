[project]
name = "five"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bm25s>=0.2.12",
    "click>=8.2.0",
    "pydantic>=2.11.4",
    "pyyaml>=6.0.2",
]

[project.optional-dependencies]
dev = [
    "black",
    "isort",
]

[tool.isort]
profile = "black"

[tool.black]
line-length = 90
