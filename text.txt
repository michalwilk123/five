
{
    "symbol": file,
    "filename": hbjkmk
    "content": [
        {
            "symbol": file,
            "filename": bobra.txt
            "content": {
                "symbol": component,
                "content": [
                    {"symbol": text, content: "hello world"}
                ]
            }
        }
    ]
}

use
module
choice
sequence
regex
text

# syntax for events:
<regex name="sth" value="{vname}" event_5_on-delete="@some_lib/func"/>


generate will take list of patches to generate code, for interactive 
mode it will return the patches that it has missing


Mozna zrobic cos w stylu pola "count" ktore okresla ile razy dany obiekt moze sie pojawic. Skladnia jaka sie dodaje
jest podobna do wykresow zw. z kardynalność w grafach systemow rdbms.

Domyslnie ustawiona jest wartość: 1

np.
0
0..1
1..
3..5
0..2
